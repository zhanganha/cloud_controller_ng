---
applications:
  .:
    name: ccng
    command: bin/cloud_controller -m
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      postgresql-1a0bb:
        type: postgresql
